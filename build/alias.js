const path = require('path');

module.exports = {
    api: path.resolve(__dirname, '../Api'),
    middlewares: path.resolve(__dirname, '../middlewres'),
    controllers: path.resolve(__dirname, '../controllers')
}
